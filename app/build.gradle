plugins {
    id 'com.android.application'
}

android {
    namespace 'com.swill.injector'
    compileSdk 34

    defaultConfig {
        applicationId "com.swill.injector"
        minSdk 26
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    // ОТКЛЮЧАЕМ NDK полностью
    sourceSets {
        main {
            jni.srcDirs = []
            jniLibs.srcDirs = []
        }
    }

    // Запрещаем автоматический вызов ndk-build
    packagingOptions {
        exclude '**/lib/*.so'
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}
